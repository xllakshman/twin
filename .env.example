# AWS Configuration
AWS_ACCOUNT_ID=514041285511
DEFAULT_AWS_REGION=ap-south-1

# Project Configuration
PROJECT_NAME=twin

# OpenAI (dev fallback — do not commit real keys)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
LLM_PROVIDER=openai_with_bedrock_fallback
